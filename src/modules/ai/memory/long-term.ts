/**
 * Resumos de longo prazo (semanal/mensal) por criança.
 * Onda 1: stub — retorna string vazia.
 * Onda 2: job diário grava em `ai_summaries`; esta função lê o mais recente.
 */
export async function getLatestSummary(_subjectId: string): Promise<string> {
  return "";
}
